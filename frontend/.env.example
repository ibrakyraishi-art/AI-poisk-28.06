# Frontend environment variables — set these in Vercel project settings
# For local dev: copy to frontend/.env.local

# === Supabase (public keys — safe to expose to browser) ===
NEXT_PUBLIC_SUPABASE_URL=https://nqpeidlfkcxucgqykyrv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here   # Dashboard → Settings → API → anon/public

# === Backend API URL ===
NEXT_PUBLIC_API_URL=https://your-railway-app.up.railway.app
