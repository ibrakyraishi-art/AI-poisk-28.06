# ── Core AI framework ───────────────────────────────────────────────────────
# ВАЖНО: зафиксировать точную версию до Stage 4 (ConditionalTask API нестабилен)
# После первой установки запусти: pip freeze | grep crewai >> requirements.txt
crewai>=0.80.0,<1.0.0
crewai-tools>=0.14.0

# ── Embeddings (локально, без API-ключей) ────────────────────────────────────
sentence-transformers>=2.7.0

# ── Database ─────────────────────────────────────────────────────────────────
supabase>=2.0.0

# ── Scrapers (нестабильны — mock-фикстура в tests/fixtures/) ─────────────────
google-play-scraper>=1.2.0
app-store-scraper>=0.3.0

# ── Report generation (Stage 3) ──────────────────────────────────────────────
python-docx>=1.1.0

# ── News API (Stage 3) ───────────────────────────────────────────────────────
newsapi-python>=0.2.7

# ── Web search — SerperDev (Stage 5) ─────────────────────────────────────────
serper-python>=0.1.0
