# ── Web server ───────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# [crypto] extra pulls `cryptography` — required to verify Supabase's asymmetric
# (ES256/RS256) JWTs via JWKS, not just legacy HS256.
PyJWT[crypto]>=2.8.0

# ── Core AI framework ───────────────────────────────────────────────────────
# <1.0.0 обязательно: crewai 1.x сломал три вещи, от которых мы зависим:
#   1. output_pydantic= убран из Task (заменён на structured output иного вида)
#   2. ConditionalTask перенесён / переименован — Stage 4 сломается
#   3. LLM-провайдеры теперь отдельные extras (crewai[anthropic] и т.д.),
#      поэтому наши litellm-строки ("claude-haiku-4-5-20251001") перестают работать
# Поднимать до 1.x только после проверки всех трёх пунктов и миграции кода.
crewai==0.86.0

# ── Embeddings (локально, без API-ключей) ────────────────────────────────────
sentence-transformers>=2.7.0

# ── Database ─────────────────────────────────────────────────────────────────
supabase>=2.0.0

# ── Scrapers (нестабильны — mock-фикстура в tests/fixtures/) ─────────────────
# app-store-scraper removed: pins requests==2.23.0, conflicts with auth0-python>=2.31.0
google-play-scraper>=1.2.0

# ── Report generation (Stage 3) ──────────────────────────────────────────────
python-docx>=1.1.0

# ── News API (Stage 3) ───────────────────────────────────────────────────────
newsapi-python>=0.2.7

# ── Web search — SerperDev (Stage 5) ─────────────────────────────────────────
# requests comes in as transitive dep — no explicit pin needed
